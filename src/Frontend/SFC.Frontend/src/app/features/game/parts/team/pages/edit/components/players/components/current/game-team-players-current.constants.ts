import { IPaginationModel } from "ngx-sfc-common";

export class GameTeamPlayersCurrentConstants {
    static PAGINATION: IPaginationModel = { page: 0, size: 5 };
    static TableContentRadius: number = 35;
}