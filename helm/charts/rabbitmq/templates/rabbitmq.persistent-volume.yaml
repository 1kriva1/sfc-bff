apiVersion: v1
kind: PersistentVolume
metadata:
  name: sfc-rabbitmq-persistent-volume
  labels:
    app.kubernetes.io/name: sfc-rabbitmq
    app.kubernetes.io/component: storage
    app.kubernetes.io/part-of: sfc
    app.kubernetes.io/instance: production
    app.kubernetes.io/managed-by: kubectl
  annotations:
    owner: "krivorukandrey@gmail.com"
    purpose: "Persistent volume for Rabbit MQ component in SFC application"
spec:
  capacity:
    storage: 2Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: sfc-rabbitmq-storage-class
  hostPath: #it's not recommended for production, but it's simple for demo purposes. In production, consider using a cloud provider's storage solution or a networked storage system.'
    path: "/mnt/data/rabbitmq"
    type: DirectoryOrCreate  
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
          - key: kubernetes.io/hostname
            operator: In
            values: ["desktop-worker"]